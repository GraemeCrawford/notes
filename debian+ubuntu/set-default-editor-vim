Usually the default editor will be set to nano - which I find extremely annoying when trying to do things like ```visudo```, it uses nano and I don't know how to use it!

To change this to use vim by default:

```update-alternatives --config editor```

And choose the option for vi/vim
